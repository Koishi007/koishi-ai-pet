# LLM 工具调用框架

## 架构总览

```
启动时:
  SkillLoader 扫描 pet/skills/*.py
    → 检查 SKILL_NAME 属性
    → 比对 config.SKILLS_ENABLED 配置
    → 对启用的 skill 调用 register(TOOL_REGISTRY)

LLM 输出解析:
  ├── Speech: → SpeechBubble
  ├── Action: → ActionQueue
  └── Tool: {"name": "...", "args": {...}} → ToolExecutor
        │
        ▼
     执行工具 → 结果 → 二次 LLM 调用（带 Tool result）
        │
        ▼
     最终 Speech + Action
```

**核心模块**:
- `pet/skills/__init__.py` — SkillLoader（扫描+选择性加载）
- `pet/skills/registry.py` — 工具注册表（自动生成 prompt 描述）
- `pet/skills/executor.py` — 工具执行器（解析 JSON、路由、错误处理）
- `pet/skills/system_monitor.py` — 系统监控（改造为插件接口）
- `pet/skills/browser.py` — 浏览器操作工具（新建）
- `pet/skills/file_ops.py` — 文件操作工具（新建）
- 修改 `behavior.py` — 两阶段调用逻辑
- 修改 `prompts.py` — 注入可用工具列表
- 修改 `config.py` — 新增 SKILLS_ENABLED 配置

---

## Task 1: 配置项 — config.py / .env

**文件**: `d:\qoder\DeskPet\config.py`

新增配置：
```python
SKILLS_ENABLED: list[str]  # 从 .env 读取，逗号分隔
```

**文件**: `d:\qoder\DeskPet\.env.example`

新增：
```env
# 启用的技能插件（逗号分隔，*=全部启用，留空=全部禁用）
SKILLS_ENABLED=system_monitor,browser,file
```

---

## Task 2: 工具注册表 `pet/skills/registry.py`

```python
"""工具注册表 — 自动发现、注册、描述可用工具。"""

from dataclasses import dataclass, field
from typing import Callable, Any


@dataclass
class ToolMethod:
    name: str                    # "get_stats"
    description: str             # "获取CPU和内存使用率"
    args: dict = field(default_factory=dict)  # {"interval": "采样间隔(秒), 可选"}
    handler: Callable = None     # 实际执行函数


@dataclass
class ToolDef:
    name: str                    # "system_monitor"
    description: str             # "系统资源监控"
    methods: dict[str, ToolMethod] = field(default_factory=dict)


class ToolRegistry:
    """全局工具注册表。"""

    def __init__(self):
        self._tools: dict[str, ToolDef] = {}

    def register(self, tool_name: str, description: str) -> "ToolDef":
        """注册一个工具类别。"""
        tool = ToolDef(name=tool_name, description=description)
        self._tools[tool_name] = tool
        return tool

    def add_method(self, tool_name: str, method_name: str,
                   description: str, handler: Callable, args: dict = None):
        """为工具添加一个可调用方法。"""
        tool = self._tools[tool_name]
        tool.methods[method_name] = ToolMethod(
            name=method_name,
            description=description,
            args=args or {},
            handler=handler,
        )

    def get_handler(self, full_name: str) -> Callable | None:
        """通过 'tool_name.method_name' 获取 handler。"""
        parts = full_name.split(".", 1)
        if len(parts) != 2:
            return None
        tool_name, method_name = parts
        tool = self._tools.get(tool_name)
        if not tool:
            return None
        method = tool.methods.get(method_name)
        return method.handler if method else None

    def generate_prompt_section(self) -> str:
        """生成注入 LLM prompt 的工具描述段。"""
        if not self._tools:
            return ""
        lines = ["=== 可用工具 ===",
                 "当你需要获取信息时，可以调用工具。输出格式：",
                 '  Tool: {"name": "tool.method", "args": {}}',
                 "",
                 "可用工具列表："]
        for tool in self._tools.values():
            lines.append(f"\n【{tool.name}】{tool.description}")
            for m in tool.methods.values():
                args_desc = ", ".join(f"{k}: {v}" for k, v in m.args.items())
                args_part = f"  参数: {{{args_desc}}}" if args_desc else "  无参数"
                lines.append(f"  - {tool.name}.{m.name}: {m.description}")
                lines.append(f"    {args_part}")
        return "\n".join(lines)

    @property
    def tool_names(self) -> list[str]:
        return list(self._tools.keys())


# 全局单例
TOOL_REGISTRY = ToolRegistry()
```

---

## Task 3: 插件加载器 `pet/skills/__init__.py`

```python
"""技能插件加载器 — 自动发现 + 配置选择性加载。"""

import importlib
import logging
import os
from pathlib import Path

from pet.skills.registry import TOOL_REGISTRY

logger = logging.getLogger(__name__)

# 排除的文件名（非插件）
_SKIP_FILES = {"__init__", "registry", "executor"}


def load_skills(enabled: list[str]):
    """扫描 skills 目录，按配置加载启用的插件。

    Args:
        enabled: 启用列表。["*"] 表示全部启用，[] 表示全部禁用。
    """
    if not enabled:
        logger.info("[SkillLoader] No skills enabled")
        return

    skills_dir = Path(__file__).parent
    loaded = []

    for py_file in sorted(skills_dir.glob("*.py")):
        module_name = py_file.stem
        if module_name in _SKIP_FILES:
            continue

        # 尝试读取模块的 SKILL_NAME
        try:
            module = importlib.import_module(f"pet.skills.{module_name}")
        except Exception as e:
            logger.warning(f"[SkillLoader] Failed to import {module_name}: {e}")
            continue

        skill_name = getattr(module, "SKILL_NAME", None)
        if not skill_name:
            continue

        # 检查是否启用
        if "*" not in enabled and skill_name not in enabled:
            logger.debug(f"[SkillLoader] Skip disabled skill: {skill_name}")
            continue

        # 调用 register 函数
        register_fn = getattr(module, "register", None)
        if not callable(register_fn):
            logger.warning(f"[SkillLoader] {module_name} has no register() function")
            continue

        try:
            register_fn(TOOL_REGISTRY)
            loaded.append(skill_name)
            logger.info(f"[SkillLoader] ✓ Loaded skill: {skill_name}")
        except Exception as e:
            logger.error(f"[SkillLoader] ✗ Failed to register {skill_name}: {e}")

    logger.info(f"[SkillLoader] {len(loaded)} skills loaded: {loaded}")
```

---

## Task 4: 工具执行器 `pet/skills/executor.py`

```python
"""工具执行器 — 解析 LLM 输出中的 Tool JSON，路由执行，返回结果。"""

import json
import logging
from typing import Any

from pet.skills.registry import TOOL_REGISTRY

logger = logging.getLogger(__name__)


@dataclass
class ToolCall:
    name: str       # "system_monitor.get_stats"
    args: dict      # {}


@dataclass
class ToolResult:
    name: str
    success: bool
    data: Any = None
    error: str = ""


class ToolExecutor:
    """工具执行器 — 串行执行，架构可扩展为并行。"""

    def execute(self, calls: list[ToolCall]) -> list[ToolResult]:
        """串行执行工具调用列表，失败静默跳过。"""
        results = []
        for call in calls:
            result = self._execute_one(call)
            results.append(result)
        return results

    def _execute_one(self, call: ToolCall) -> ToolResult:
        handler = TOOL_REGISTRY.get_handler(call.name)
        if handler is None:
            logger.warning(f"[ToolExecutor] unknown tool: {call.name}")
            return ToolResult(name=call.name, success=False, error="unknown tool")
        try:
            data = handler(**call.args)
            logger.info(f"[ToolExecutor] ✓ {call.name} → {str(data)[:100]}")
            return ToolResult(name=call.name, success=True, data=data)
        except Exception as e:
            logger.error(f"[ToolExecutor] ✗ {call.name} failed: {e}")
            return ToolResult(name=call.name, success=False, error=str(e))

    @staticmethod
    def parse_tool_lines(content: str) -> list[ToolCall]:
        """从 LLM 输出中解析 Tool: 行。"""
        calls = []
        for line in content.split("\n"):
            line = line.strip()
            if line.lower().startswith("tool:"):
                raw = line.split(":", 1)[1].strip()
                try:
                    obj = json.loads(raw)
                    calls.append(ToolCall(
                        name=obj.get("name", ""),
                        args=obj.get("args", {}),
                    ))
                except json.JSONDecodeError:
                    logger.warning(f"[ToolExecutor] invalid JSON: {raw[:80]}")
        return calls

    @staticmethod
    def format_results(results: list[ToolResult]) -> str:
        """将执行结果格式化为注入下一轮 prompt 的文本。"""
        lines = []
        for r in results:
            if r.success:
                lines.append(f"Tool({r.name}): {json.dumps(r.data, ensure_ascii=False)}")
            else:
                lines.append(f"Tool({r.name}): [failed] {r.error}")
        return "\n".join(lines)
```

---

## Task 5: 改造 system_monitor 为插件接口

**文件**: `pet/skills/system_monitor.py`

改造为统一的插件格式（新增 `SKILL_NAME` + `register(registry)` 接口）：

```python
"""系统监控技能 —— 监控 CPU、内存等系统指标。"""

import psutil
import logging

logger = logging.getLogger(__name__)

SKILL_NAME = "system_monitor"
SKILL_DESCRIPTION = "系统资源监控（CPU、内存使用率）"


class SystemMonitor:
    def __init__(self):
        self._enabled = True

    def get_cpu_usage(self) -> float:
        return psutil.cpu_percent(interval=0.1)

    def get_memory_usage(self) -> float:
        return psutil.virtual_memory().percent

    def get_stats(self) -> dict:
        return {
            "cpu": self.get_cpu_usage(),
            "memory": self.get_memory_usage(),
        }


# 模块级实例（加载时创建）
_instance = SystemMonitor()


def register(registry):
    """插件接口 — 由 SkillLoader 调用。"""
    registry.register(SKILL_NAME, SKILL_DESCRIPTION)
    registry.add_method(
        SKILL_NAME, "get_stats",
        "获取当前CPU使用率和内存使用率",
        handler=_instance.get_stats,
    )
    registry.add_method(
        SKILL_NAME, "get_cpu_usage",
        "获取当前CPU使用率(%)",
        handler=_instance.get_cpu_usage,
    )
    registry.add_method(
        SKILL_NAME, "get_memory_usage",
        "获取当前内存使用率(%)",
        handler=_instance.get_memory_usage,
    )
```

---

## Task 6: 浏览器操作工具 `pet/skills/browser.py`

```python
"""浏览器操作工具 — 打开URL、搜索。"""

import logging
import webbrowser

logger = logging.getLogger(__name__)

SKILL_NAME = "browser"
SKILL_DESCRIPTION = "浏览器操作（打开网页、搜索）"


class BrowserTool:
    def open_url(self, url: str) -> dict:
        """打开指定网址。"""
        webbrowser.open(url)
        return {"status": "opened", "url": url}

    def search(self, query: str) -> dict:
        """用默认浏览器搜索。"""
        url = f"https://www.bing.com/search?q={query}"
        webbrowser.open(url)
        return {"status": "searching", "query": query, "url": url}


_instance = BrowserTool()


def register(registry):
    """插件接口 — 由 SkillLoader 调用。"""
    registry.register(SKILL_NAME, SKILL_DESCRIPTION)
    registry.add_method(
        SKILL_NAME, "open_url",
        "用默认浏览器打开指定URL",
        handler=_instance.open_url,
        args={"url": "要打开的网址"},
    )
    registry.add_method(
        SKILL_NAME, "search",
        "用默认浏览器搜索关键词",
        handler=_instance.search,
        args={"query": "搜索关键词"},
    )
```

---

## Task 7: 文件操作工具 `pet/skills/file_ops.py`

```python
"""文件操作工具 — 读取、列目录、写笔记。"""

import os
import logging

logger = logging.getLogger(__name__)

SKILL_NAME = "file"
SKILL_DESCRIPTION = "文件操作（读取、列目录、写笔记，限桌面/文档）"

# 安全限制：只允许操作用户桌面和文档目录
_ALLOWED_ROOTS = [
    os.path.expanduser("~/Desktop"),
    os.path.expanduser("~/Documents"),
]


class FileOpsTool:
    def _check_path(self, path: str) -> str:
        abs_path = os.path.abspath(os.path.expanduser(path))
        if not any(abs_path.startswith(root) for root in _ALLOWED_ROOTS):
            raise PermissionError(f"不允许访问: {abs_path}")
        return abs_path

    def list_dir(self, path: str = "~/Desktop") -> dict:
        abs_path = self._check_path(path)
        if not os.path.isdir(abs_path):
            return {"error": "目录不存在"}
        items = os.listdir(abs_path)[:30]
        return {"path": abs_path, "items": items, "count": len(items)}

    def read_file(self, path: str, max_chars: int = 500) -> dict:
        abs_path = self._check_path(path)
        if not os.path.isfile(abs_path):
            return {"error": "文件不存在"}
        try:
            with open(abs_path, "r", encoding="utf-8") as f:
                content = f.read(max_chars)
            return {"path": abs_path, "content": content, "truncated": len(content) >= max_chars}
        except Exception as e:
            return {"error": str(e)}

    def write_note(self, filename: str, content: str) -> dict:
        desktop = os.path.expanduser("~/Desktop")
        path = os.path.join(desktop, filename)
        abs_path = self._check_path(path)
        with open(abs_path, "w", encoding="utf-8") as f:
            f.write(content)
        return {"status": "written", "path": abs_path}


_instance = FileOpsTool()


def register(registry):
    """插件接口 — 由 SkillLoader 调用。"""
    registry.register(SKILL_NAME, SKILL_DESCRIPTION)
    registry.add_method(
        SKILL_NAME, "list_dir",
        "列出指定目录内容（限桌面/文档）",
        handler=_instance.list_dir,
        args={"path": "目录路径，默认桌面"},
    )
    registry.add_method(
        SKILL_NAME, "read_file",
        "读取文本文件前500字符（限桌面/文档）",
        handler=_instance.read_file,
        args={"path": "文件路径", "max_chars": "最大读取字符数(可选)"},
    )
    registry.add_method(
        SKILL_NAME, "write_note",
        "在桌面创建一个文本笔记",
        handler=_instance.write_note,
        args={"filename": "文件名(含扩展名)", "content": "内容"},
    )
```

---

## Task 8: behavior.py — 两阶段调用逻辑

**文件**: `d:\qoder\DeskPet\pet\brain\behavior.py`

### 6.1 新增 _execute_with_tools 方法

在 `Behavior` 类中添加统一的两阶段调用封装：

```python
def _execute_with_tools(self, first_content: str, system_content: str) -> BehaviorOutput:
    """解析 LLM 输出，若含 Tool 调用则执行工具并进行二次调用。"""
    from pet.skills.executor import ToolExecutor

    executor = ToolExecutor()
    tool_calls = executor.parse_tool_lines(first_content)

    if not tool_calls:
        # 无工具调用，直接解析行为
        return self._parse_behavior(first_content)

    # 执行工具
    results = executor.execute(tool_calls)
    result_text = executor.format_results(results)

    # 二次调用 LLM（带工具结果）
    messages = [
        {"role": "system", "content": system_content},
        {"role": "assistant", "content": first_content},
        {"role": "user", "content": f"=== 工具执行结果 ===\n{result_text}\n\n"
                                     "根据工具结果，输出最终的 Speech + Action。不要再调用工具。"},
    ]
    try:
        resp = self._client.chat.completions.create(
            model=self._model,
            messages=messages,
            max_tokens=4000,
        )
        final_content = resp.choices[0].message.content or ""
        return self._parse_behavior(final_content)
    except Exception as e:
        logger.error(f"[Behavior] tool second-pass failed: {e}")
        # 降级：用第一轮的 Speech/Action（忽略 Tool 行）
        return self._parse_behavior(first_content)
```

### 6.2 修改现有 LLM 调用方法

在 `_decide_non_vision`、`_decide_vision`、`_chat_decide_remote` 中，将原来的：
```python
result = self._parse_behavior(content)
```

替换为：
```python
result = self._execute_with_tools(content, system_content)
```

这样三个 tick 频率触发的 decide 和 chat 都支持工具调用。

---

## Task 9: prompts.py — 注入工具描述

**文件**: `d:\qoder\DeskPet\pet\brain\prompts.py`

在 `non_vision_system_prompt()`、`vision_system_prompt()`、`chat_decide_system_prompt()` 中插入工具段：

```python
from pet.skills.registry import TOOL_REGISTRY

# 在各 system_prompt 函数的返回值中追加：
tools_section = TOOL_REGISTRY.generate_prompt_section()
if tools_section:
    system_content += f"\n\n{tools_section}"
```

同时在 `_COMMON_TAIL` 的格式说明中补充：

```
Tool: {"name": "tool.method", "args": {...}}   ← 可选，需要获取信息时使用
```

---

## Task 10: main.py — 启动时加载插件

**文件**: `d:\qoder\DeskPet\main.py`

替换原有手动注册逻辑，改为一行加载：

```python
from pet.skills import load_skills
from config import config

# 启动时加载技能插件
load_skills(config.SKILLS_ENABLED)
```

不再需要 `from pet.skills.xxx import ...` 和手动 `register_xxx()` 调用。

---

## 扩展性说明

**加新 skill 只需**：
1. 在 `pet/skills/` 下创建 `.py` 文件
2. 定义 `SKILL_NAME` 常量和 `register(registry)` 函数
3. `.env` 中 `SKILLS_ENABLED` 加上对应名字（或用 `*`）

**串行→并行扩展点**：`ToolExecutor.execute()` 内部改为 `concurrent.futures.ThreadPoolExecutor` 即可，外部接口不变。

**LLM prompt 自动更新**：`TOOL_REGISTRY.generate_prompt_section()` 动态生成，插件加载后 prompt 自动包含新工具。

**禁用 skill**：`.env` 中删掉名字即可，文件不用动，重启生效。